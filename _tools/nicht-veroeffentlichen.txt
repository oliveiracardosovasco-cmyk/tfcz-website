# ============================================================================
# TFCZ · Was NIE auf eine öffentliche Seite gehört — die EINE Liste.
#
# Vorher stand dieselbe Aufzählung dreimal und überall anders:
#   · die rm-Liste in .github/workflows/pages.yml   (GitHub Pages)
#   · .dockerignore im Vereins-Repo ocsav           (tfcz.ch)
#   · beim Spiegeln nach ocsav: gar nicht
# Darum wäre am 01.09.2026 beinahe `_to_delete/` live gegangen — es stand in
# keiner der beiden Listen. Ab jetzt liest _tools/veroeffentlichen.mjs von hier,
# und pages.yml ebenso.
#
# Regeln:
#   · Eine Zeile = ein Eintrag. `#` beginnt einen Kommentar.
#   · OHNE Schrägstrich  = Name, greift auf JEDER Ebene (`_to_delete` erwischt
#     auch assets/img/_to_delete — genau der Fall, der oben durchgerutscht ist).
#   · MIT Schrägstrich   = Pfad ab der Wurzel der Seite.
#   · `*` am Anfang oder Ende = Platzhalter (`*.bak`).
# ============================================================================

# --- Geheim: was nicht im Netz stehen darf ---
_secret                       # Eier-Jagd im Klartext (§3b CLAUDE.md)
config.example.js             # Vorlage mit Feldnamen der Konfiguration

# --- Aussortiertes: bleibt lokal (Vasco 16.08.2026) ---
_to_delete
_backup-vor-reset
_archive
_snapshots
_snapshot-baseline

# --- Werkzeug und Gerüst: nichts davon wird ausgeliefert ---
_tools
tools
_module                       # geparkte Batch-Module, nicht aktiv
node_modules
.github
.git
.gitignore                    # nennt _secret/ samt Begründung — verrät die Jagd

# --- Interne Doku: beschreibt Aufbau und Absichten ---
CLAUDE.md
README.md
API.md
ARCHITEKTUR.md
TODO.md
CTA-ANALYSE.md
SCHILDER.md
UMBAU-NAVIGATION-Variante-2.md
okapi-event-mapping.md
docs
package.json
package-lock.json

# --- Arbeitskopien und Betriebssystem-Müll ---
*.bak
*.bak-*
.DS_Store
.fuse_hidden*
*:Zone.Identifier
